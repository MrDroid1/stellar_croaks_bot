version: '3.8'

services:
  bot:
    build: .
    container_name: stellar-croak-bot
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      - ./logs:/app/logs
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
